import { describe, expect, it } from "@jest/globals"

describe("index.ts", () => {
	it("generates Crates", () => {
		// expect()
	})
	it("day 5 1. Half", async () => {
		// preprocess

	})
	// it("day 5 2. Half", async () => {})
})
